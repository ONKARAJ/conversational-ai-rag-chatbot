# Only needed if you serve the API from a different origin than the frontend.
# Left empty, the app calls relative /api paths and Vite proxies them to
# http://localhost:8000 (see vite.config.js).
VITE_API_BASE_URL=

# Where the Vite dev proxy forwards /api requests.
VITE_BACKEND_URL=http://localhost:8000
