# --- Web framework -------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- Storage -------------------------------------------------------------
SQLAlchemy==2.0.36
# For PostgreSQL later, add:  psycopg[binary]==3.2.3

# --- LangChain + Groq (the notebook's stack) -----------------------------
langchain==0.3.14
langchain-core==0.3.29
langchain-community==0.3.14
langchain-groq==0.2.3
langchain-text-splitters==0.3.5
tiktoken==0.8.0

# --- RAG: embeddings + vector store --------------------------------------
# These are the heavy ones (they pull in torch). Remove them and set
# RAG_ENABLED=false in .env if you only want the chat side running.
langchain-huggingface==0.1.2
sentence-transformers==3.3.1
langchain-chroma==0.2.0
chromadb==0.5.23
